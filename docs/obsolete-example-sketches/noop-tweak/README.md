# NoOpTweak Historical Makefiles

These Makefile snapshots record earlier experiments from before the current Mach-O stub and wrapper strategy stabilized.

They are not part of the active validation pipeline.

The active no-op example is:

    examples/noop-tweak/Makefile
